mixin card(title, img, platform, date, description, score, rankNumber)
  div.videogameCard
    h3= title
    p= rankNumber
    p= score
    p= platform
    p= date
    p= description
    img(src= img)
